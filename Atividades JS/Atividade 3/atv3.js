{
    let mensagem = 'Manipulando strings com JS'.toUpperCase();
    let mensagem_2 = 'Vamos concatenar as duas strings';
    alert(mensagem)
    
    let slice = mensagem.slice(0,5)
    console.log(slice)

    alert(mensagem.length)

    console.log(`${mensagem}, ${mensagem_2}`)
}   